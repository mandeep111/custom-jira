import React from 'react';
import { Logout } from '../../containers/Authentication';

const Page = () => {

    return (
        <React.Fragment>
            <Logout />
        </React.Fragment>
    );
};

export default Page;